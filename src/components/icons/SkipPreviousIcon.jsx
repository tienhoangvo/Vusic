import OutlinedIcon from "./OutlinedIcon";

const SkipPreviousIcon = ({ fontSize = "small", color = "inherit" }) => {
  return (
    <OutlinedIcon fontSize={fontSize} color={color}>
      skip_previous
    </OutlinedIcon>
  );
};

export default SkipPreviousIcon;
