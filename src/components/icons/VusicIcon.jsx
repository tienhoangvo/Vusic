import OutlinedIcon from "./OutlinedIcon";

const VusicIcon = ({ fontSize = "small", color = "inherit" }) => {
  return (
    <OutlinedIcon fontSize={fontSize} color={color}>
      headphones
    </OutlinedIcon>
  );
};

export default VusicIcon;
