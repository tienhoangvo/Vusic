import OutlinedIcon from "./OutlinedIcon";

const SkipNextIcon = ({ fontSize = "small", color = "inherit" }) => {
  return (
    <OutlinedIcon fontSize={fontSize} color={color}>
      skip_next
    </OutlinedIcon>
  );
};

export default SkipNextIcon;
