import OutlinedIcon from "./OutlinedIcon";

const PlaylistAddIcon = ({ fontSize = "small", color = "inherit" }) => {
  return (
    <OutlinedIcon fontSize={fontSize} color={color}>
      playlist_add
    </OutlinedIcon>
  );
};

export default PlaylistAddIcon;
